#include <stdint.h>
#include <stdio.h>

uint8_t arr[4] = {1, 2, 3, 4};

uint8_t add = 0x0;

int main() {
  uint8_t* arr_p = arr + add;
  printf("%p\n", arr);
  printf("%p\n", arr_p);
  printf("%d\n", *arr_p);
}
